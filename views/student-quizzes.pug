extends layout

block content
  .page-header
    h1 📚 Available Quizzes

  .page-container
    if quizzes && quizzes.length > 0
      .quiz-grid
        each quiz in quizzes
          .quiz-card
            h2= quiz.title
            p.quiz-description= quiz.description || 'No description'
            .quiz-meta
              p 👨‍🏫 Instructor: #{quiz.instructor_name}
              p 📝 Questions: #{quiz.question_count || 0}
              if quiz.time_limit
                p ⏱️ Time Limit: #{quiz.time_limit} minutes
              p 📅 Created: #{new Date(quiz.created_at).toLocaleDateString()}
            a.btn-take-quiz(href=`/student/quizzes/${quiz.id}/take`) Take Quiz
    else
      .empty-state
        p No quizzes available yet.

    .results-link
      a(href='/student/results') 📊 View My Results History
